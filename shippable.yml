language: python

python:
  - 2.6

build:
  ci:
    - pip install codecov
    - if [ $((RANDOM % 2)) ]; then pip install rsa; else pip install PyCrypto; fi
    - coverage run -a py/test.py
  on_success:
    - coverage combine
    - codecov --token=d89f9bd9-27a3-4560-8dbb-39ee3ba020a5
